#!/bin/bash
#MISE description="Setup Direnv"
set -euo pipefail

bashrc="/etc/bash.bashrc"
line='eval "$(/usr/local/bin/direnv hook bash)"'

if ! grep -Fqx "$line" "$bashrc"; then
  echo "$line" >> "$bashrc"
fi
