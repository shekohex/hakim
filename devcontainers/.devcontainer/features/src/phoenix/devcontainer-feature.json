{
    "name": "Phoenix Framework",
    "id": "phoenix",
    "version": "1.0.0",
    "description": "Installs the Phoenix installer (phx_new) and OS dependencies for file watching. Uses user home for Mix archives.",
    "options": {
    "version": {
      "type": "string",
      "proposals": [
        "latest",
        "1.7.18",
        "1.7.17",
        "1.7.16"
      ],
      "default": "1.8.3",
      "description": "Select the version of phx_new to install."
    },
        "seedUserHome": {
            "type": "boolean",
            "default": true,
            "description": "Install phx_new into the runtime user home during image build."
        }
    },
    "dependsOn": {
        "../../../features/src/elixir": {}
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils",
        "../../../features/src/elixir"
    ]
}
