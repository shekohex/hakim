{
    "name": "Elixir (via Mise)",
    "id": "elixir",
    "version": "1.0.0",
    "description": "Installs Erlang/OTP and Elixir using Mise. Configures Mix/Hex to use user home directories.",
    "options": {
        "erlangVersion": {
            "type": "string",
            "proposals": [
                "27",
                "26",
                "latest"
            ],
            "default": "latest",
            "description": "Select the version of Erlang/OTP to install."
        },
        "elixirVersion": {
            "type": "string",
            "proposals": [
                "1.17",
                "1.16",
                "latest"
            ],
            "default": "latest",
            "description": "Select the version of Elixir to install."
        },
        "seedUserHome": {
            "type": "boolean",
            "default": true,
            "description": "Install Hex/Rebar into the runtime user home during image build."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
