{
  "name": "UV",
  "id": "uv",
  "version": "1.0.0",
  "description": "Installs uv and Python via uv.",
  "options": {
    "pythonVersion": {
      "type": "string",
      "proposals": [
        "3.12",
        "latest"
      ],
      "default": "3.12.12",
      "description": "Select the Python version to install via uv."
    },
    "uvVersion": {
      "type": "string",
      "default": "0.9.28",
      "description": "Select the uv version to install."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils",
    "../../mise"
  ]
}
